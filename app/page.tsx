import { LiveSearchDebounce } from '@/components/LiveSearch/live-search-with-debounce';
import { LiveSearchWithUseDeferredValue } from '@/components/LiveSearch/live-search-with-use-deferred-value';

export default function Home() {
  return (
    <main className='flex min-h-screen flex-col items-center justify-between p-24'>
      {/* <LiveSearchDebounce /> */}
      {/* <LiveSearchWithUseDeferredValue /> */}
    </main>
  );
}
